# Auto-Analyst
I built an agent that takes any CSV, writes its own analysis code, runs it, fixes errors automatically, and explains the findings — all without human input. The interesting part was designing the self-healing loop using LangGraph's conditional edges.
